# TH_Tried

간단한 미연시(비주얼 노블) 엔진 예제입니다. `game.py` 실행 시 필요한 `assets/`, `font/` 폴더와 기본 `scenes.json` 파일을 자동으로 생성합니다.

## 실행 방법

1. `pip install pygame`
2. `python game.py`
3. 생성된 `scenes.json`과 `assets/` 폴더를 편집하여 스토리와 이미지를 추가합니다.
